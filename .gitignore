node_modules/
dist/
.env
*.log

# Dev restart-guard lock (scripts/dev-bot.mjs <-> src/core/devGuard.ts)
.dev-busy
uploads/
.DS_Store

# Locally downloaded Vosk speech models (installer puts them here)
models/

# User/agent-created files land here (default WORKDIR) — keep them out of the repo
data/
!data/.gitkeep

# Persistent log files (NDJSON, rotated every 72h)
logs/

# Machine-local Claude Code settings (keep personal allowlists out of the repo)
.claude/settings.local.json
# Agent worktrees and other machine-local Claude Code scratch
.claude/

# TypeScript incremental build cache
*.tsbuildinfo

# Launch/marketing docs — kept locally, not shipped in the repo
docs/
